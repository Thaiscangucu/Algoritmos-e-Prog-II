#include <stdio.h>
#include <math.h>

int main() {
    float r;
    float a; 
    printf("Raio em metros:");
    scanf ("%f", &r);
    a = M_PI*(r*r);
    printf("Um círculo com raio: %.1f m tem uma área igual a: %.1f m", r, a);

    return 0;
}

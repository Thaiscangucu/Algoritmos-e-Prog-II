#include <stdio.h>
#include <math.h>

void tabuada(int n){
  for (int i = 0; i<= 10; i++){
    printf("%d X %d = %d\n", n, i, n*i);
  }
}

int main(){
  printf("Número: ");
  int n;
  
  scanf("%d", &n);
  tabuada(n);
  return 0;
}
